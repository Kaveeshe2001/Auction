import React from 'react'
import LoginForm from './LoginForm';

const Login = () => {
    return (
        <section className='bg-gray- min-h-screen flex items-center justify-center'>
          <LoginForm />
        </section>
    );
}

export default Login
